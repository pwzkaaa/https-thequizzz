# THE QUIZ — split files

This archive contains the website files split from the original single HTML file you uploaded.

Files:
- `index.html` — main HTML (references `styles.css` and `script.js`)
- `styles.css` — combined inline styles extracted from the original HTML
- `script.js` — combined inline scripts extracted from the original HTML
- `README.md` — this file

How to use:
1. Unzip the archive.
2. Open `index.html` in a browser (double-click or open via local web server).

Notes:
- External script tags (those with `src` attributes) from the original HTML were left in `index.html` and are still referenced as before; only inline `<style>` and inline `<script>` contents were moved into `styles.css` and `script.js`.
- If you host the files on a server, make sure the external script URLs are reachable.

Generated from the uploaded file: /mnt/data/doctype html.txt
